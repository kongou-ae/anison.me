{
  "name": "itunes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "build": "./node_modules/.bin/babel src --out-dir ./dist",
    "dynamo": "npm run build && node dist/updateDynamo.js",
    "json": "npm run build && node dist/buildJson.js",
    "rss": "npm run build && node dist/rss.js",
    "release": "node_modules/.bin/netlify deploy -t $netlify_token"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-polyfill": "^6.22.0",
    "babel-preset-es2015": "^6.22.0",
    "babel-preset-stage-3": "^6.22.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.22.0",
    "fs-promise": "^2.0.0",
    "moment": "^2.17.1",
    "netlify-cli": "^1.1.0",
    "request": "^2.79.0",
    "request-promise": "^4.1.1",
    "rss": "^1.2.2",
    "serverless": "^1.5.1",
    "sleep-promise": "^2.0.0"
  }
}
