{
  "name": "itunes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "build": "./node_modules/.bin/babel src --out-dir ./",
    "db": "npm run build && node main.js",
    "release": "node_modules/.bin/netlify deploy -t $netlify_token"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-polyfill": "^6.22.0",
    "babel-preset-es2015": "^6.22.0",
    "babel-preset-stage-3": "^6.22.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs-promise": "^2.0.0",
    "netlify-cli": "^1.1.0",
    "request": "^2.79.0",
    "request-promise": "^4.1.1",
    "sleep-promise": "^2.0.0"
  }
}
