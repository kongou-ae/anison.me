machine:
  timezone: Asia/Tokyo
  node:
    version: v6.1.0
  
dependencies:
  pre:
    - npm install 

deployment:
  master:
    branch: master
    commands:
      - npm run start
      - npm run release